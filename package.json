{
  "name": "meetnote",
  "version": "1.0.0",
  "private": true,
  "description": "trans-mom - 실시간 통역과 회의록. 준실시간 전사/통역 + 화자 분리 + AI 회의록.",
  "scripts": {
    "android:init": "npx cap add android && node scripts/apply-native.js",
    "android:sync": "npx cap sync android && node scripts/apply-native.js",
    "android:build": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "serve": "npx http-server www -p 8080 -c-1"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
