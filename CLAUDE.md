# Claude Code 작업 지침

## 이 리포가 뭔지

한국어·베트남어·영어 회의를 준실시간 전사/번역하고 AI 회의록을 만드는 안드로이드 앱.
Capacitor 로 감싼 단일 HTML. 백엔드 서버 없음.

## 절대 어기면 안 되는 원칙

1. **녹음과 전사를 분리한다.** 녹음기는 로컬 저장만. 전사는 큐 워커가 별도로.
2. **오프라인을 분기로 처리하지 마라.** 큐가 안 빠지는 상태일 뿐이다.
   UI 나 녹음 로직에 온·오프라인 조건문을 넣지 말 것.
3. **파일 업로드도 같은 큐를 탄다.** 전사 경로는 하나뿐이어야 한다.
4. **녹음 스트림을 절대 끊지 마라.** AudioRecord 로 PCM 을 연속으로 받고 파일 경계만
   소프트웨어로 나눈다. MediaRecorder 를 청크마다 stop/start 하는 방식으로 되돌리면
   전환마다 0.1~0.3초가 유실된다. 1시간이면 20초 넘게 사라진다. 이건 회귀다.
5. **`android/` 를 커밋하지 마라.** `scripts/apply-native.js` 가 재현한다.

## 자주 하게 될 작업

### 웹 코드 고치기
`www/index.html` 만 고친다. 그 다음:
```bash
npm run android:sync
```

### 네이티브 고치기
`native/*.java` 를 고친 뒤 `npm run android:sync`.
`scripts/apply-native.js` 가 다시 복사하고 매니페스트를 패치한다. 멱등하다.

### APK 빌드
```bash
npm run android:build
# 산출물: android/app/build/outputs/apk/debug/app-debug.apk
```
또는 그냥 push 하면 GitHub Actions 가 빌드한다.

## 빌드가 깨지면 여기부터 본다

| 증상 | 원인 | 조치 |
|---|---|---|
| `cap add android` 실패 | Capacitor 버전 불일치 | `npm i @capacitor/core@latest @capacitor/cli@latest @capacitor/android@latest` 후 `package.json` 버전 맞추기 |
| `registerPlugin` 못 찾음 | MainActivity 패치 실패 | `scripts/apply-native.js` 의 MainActivity 분기 확인. Kotlin 으로 생성됐을 수 있다 |
| 매니페스트 병합 오류 | 권한 중복 | `apply-native.js` 는 중복 체크를 한다. 수동 편집한 게 있는지 확인 |
| 녹음은 되는데 화면 끄면 끊김 | Foreground Service 미기동 | `foregroundServiceType="microphone"` 과 `FOREGROUND_SERVICE_MICROPHONE` 권한 확인 |
| Android 14+ 에서 서비스 즉시 종료 | FGS 타입 선언 누락 | 위와 동일. `startForeground` 3-인자 버전이 호출되는지 확인 |
| 청크가 안 옴 | 리스너 등록 순서 | `Native.addListener("chunk")` 가 `Native.start()` 보다 먼저여야 한다 |
| 오디오가 잡음/깨짐 | 샘플레이트 불일치 | `RATE=16000` 이 기기에서 지원되는지 확인. 안 되면 44100 으로 받고 다운샘플 |
| 일시정지 후 재개가 안 됨 | AudioRecord 재시작 실패 | `runLoop` 의 `micActive` 분기 확인 |
| 청크가 너무 길거나 짧음 | 잡음 바닥 오판 | `noiseFloor` 계수와 `Math.max(220.0, ...)` 임계 조정. 현장이 시끄러우면 임계를 올린다 |
| 청크가 15초에서 안 잘림 | 침묵이 안 잡힘 | 조용한 회의실인데 임계가 낮으면 계속 말소리로 본다. 실측 RMS 를 로그로 찍어 볼 것 |

## 사전 자료 파일 읽기

`fileToText()` 가 확장자와 MIME 으로 분기한다. 서버가 없으므로 전부 브라우저와 API 로 푼다.

- **PDF 는 2단계다.** pdf.js 로 텍스트 레이어를 먼저 뽑고, 글자가 거의 없으면(스캔본)
  페이지를 캔버스로 렌더링해 비전으로 읽는다. 비전 경로는 15쪽으로 제한한다.
  쪽수 제한을 풀지 마라. 스캔본 50쪽이면 호출 50번에 비용도 시간도 감당이 안 된다.
- **CDN 라이브러리는 `loadLib()` 로 지연 로딩한다.** cdnjs 가 죽으면 jsdelivr 로 넘어간다.
  버전을 올릴 때는 두 CDN 의 경로 구조가 다르므로 `LIB` 의 양쪽을 다 고쳐야 한다.
- **.hwp 는 지원하지 않는다.** 바이너리 복합 포맷이라 클라이언트에서 풀 수 없다.
  안내 메시지로 PDF 내보내기나 사진 촬영을 유도한다. 이걸 억지로 시도하지 마라.

## 사전 자료가 어떻게 쓰이나

`note.brief` 에 저장된다. 두 경로로 갈라진다.

1. `primeTerms()` → `extractTerms()` 가 gpt-4o-mini 로 고유명사만 뽑아 `note.terms` 에
   저장하고, 시리즈면 `series.terms` 에도 합친다. `sttChunk()` 가 이걸 쓴다.
   **전문을 전사 프롬프트에 넣지 마라.** 길이 제한 때문에 잘려나가고 오히려 해가 된다.
2. `makeMinutes()` 가 전문을 그대로 넣고 "안건 처리 현황" 절을 요구한다.

`primeTerms()` 는 녹음을 막지 않도록 await 하지 않고 뒤에서 돈다. 첫 청크가
15초 뒤에 오므로 대개 추출이 먼저 끝난다. 안 끝나도 그 청크만 고유명사 없이 갈 뿐이다.

**프롬프트의 "자료 내용을 실제로 오간 말인 것처럼 옮겨 적지 마라" 지시를 빼지 마라.**
이게 없으면 지난 회의록이 이번 회의록에 섞인다. 실제로 일어나는 실패다.

## 회의록 형식

절 이름은 **고정이다**. `결정사항 / 미결사항 / 투두리스트 / 일반요약` 네 개가 항상
이 순서로 나오고, 조건부로 앞에 `지난 회의 후속`, 뒤에 `확인 필요` 가 붙는다.

`sectionStats()` 와 `statBar()` 가 이 이름에 의존해 개수를 센다.
프롬프트에서 절 이름을 바꾸면 지표가 조용히 0 이 된다. 바꿀 거면 `SECTION_KIND` 도
같이 고쳐라.

투두는 "담당자 — 할 일 — 기한" 세 칸을 항상 채우게 하고, 모르면 "미지정" 으로 쓰게 한다.
**항목을 빼는 것보다 미지정으로 남기는 것이 낫다는 원칙이다.** 이 지시를 빼지 마라.

## 회의 시리즈가 어떻게 동작하나

`CFG.series[]` 에 시리즈별로 `terms`(고유명사), `carry`(미결 사항), 참석자·성격이 쌓인다.

- **적립**: 회의록 프롬프트가 본문 뒤에 `---CARRY---` + JSON 블록을 요구한다.
  `absorbCarry()` 가 이 블록을 떼어내 시리즈에 반영하고, 본문만 저장한다.
- **사용**: `sttChunk()` 가 `terms` 를 전사 프롬프트 맨 앞에 넣는다.
  `makeMinutes()` 가 `carry` 를 넣고 "지난 회의 후속" 섹션을 요구한다.

주의할 점 두 가지.

1. **JSON 위생처리를 빼지 마라.** LLM 이 JSON 문자열 안에 실제 줄바꿈을 넣는 일이
   흔하고, 그대로 `JSON.parse` 하면 터진다. 따옴표 안쪽 제어문자만 이스케이프하는
   루프가 들어 있다. 파싱이 실패해도 회의록 본문은 살아남아야 한다.
2. **`terms` 는 `TERM_CAP`(40) 개로 제한한다.** 전사 API 의 프롬프트 길이가 제한적이라
   무한정 쌓으면 잘려나가고, 오인식된 말이 섞이면 오답이 계속 강화된다.
   그래서 설정에서 사용자가 직접 지울 수 있게 열어 뒀다.

## 화자 추정이 어떻게 동작하나

성문을 쓰지 않는다. 설정에 저장된 참석자 프로필(역할·언어·말투·자주 쓰는 말)을
회의록 프롬프트에 넣고 LLM 이 텍스트 근거로 추정한다.

정확도를 올리려면 프로필을 구체적으로 쓰는 것이 유일한 방법이다.
프롬프트에는 "근거가 약하면 발언자 불명으로 남겨라" 가 들어 있다. 이 지시를 빼지 마라.
틀린 이름이 붙은 회의록은 이름 없는 회의록보다 나쁘다.

진짜 성문 인식이 필요해지면 Picovoice Falcon(분리) + Eagle(대조) 를 온디바이스로
얹는다. 단, **화자 분리는 전체 오디오가 있어야 한다.** 청크별로 돌리면 청크마다
화자 번호가 새로 매겨져서 이어붙일 수 없다. 회의 종료 후 2차 패스로 설계해야 하고,
지금 전사 후 삭제하는 오디오를 보관하도록 바꿔야 한다.

## 다음에 붙일 것 (우선순위)

1. **성문 기반 화자 인식** — 위 참고. 텍스트 추정 정확도가 부족할 때만.
2. **녹음 중 앱 강제 종료 복구** — 캐시에 남은 세그먼트를 앱 재시작 시 회수.
   `RecorderPlugin.clearCache` 를 회수 로직으로 바꿔야 한다.
3. **회의록 템플릿** — 지금은 고정. 주간회의/현장점검/계약협의 등 유형별 프롬프트.
4. **다중 사용자** — 사내 배포한다면 여기서 설계가 완전히 달라진다.
   기기 로컬 저장을 버리고 서버가 필요해진다. v1 범위 밖.
